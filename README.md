# Convex Optimization Learning

Interactive notes for building geometric intuition in convex optimization.

## Current page

- `index.html`: convex functions as upper envelopes of affine supporting functions.
- `script.js`: dynamic canvas visualization for \(f(x)=x^2\).
- `styles.css`: static page styling.

## Local preview

Open `index.html` directly in a browser.
